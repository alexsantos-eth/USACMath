export const breakPoint: boolean = window.innerWidth >= 900
export const isDesktop: boolean = window.innerWidth > 500
export const breakPointMid: boolean = window.innerWidth >= 500
export const breakPointMid2: boolean = window.innerWidth >= 600
export const breakPointLarge: boolean = window.innerWidth >= 1500
export const breakPointLarge2: boolean = window.innerWidth >= 1700
