export const breakPoint: boolean = window.innerWidth >= 900 ? true : false
export const isDesktop: boolean = window.innerWidth > 500
export const breakPointMid: boolean = window.innerWidth >= 500 ? true : false
export const breakPointMid2: boolean = window.innerWidth >= 600 ? true : false
export const breakPointLarge: boolean = window.innerWidth >= 1500 ? true : false
export const breakPointLarge2: boolean = window.innerWidth >= 1700 ? true : false
